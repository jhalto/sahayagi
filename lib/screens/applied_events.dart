import 'package:flutter/material.dart';

class AppliedEvents extends StatelessWidget {
  const AppliedEvents({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Applied Events"),
      ),

    );
  }
}
